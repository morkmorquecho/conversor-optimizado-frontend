<script setup>
defineProps({})
</script>

<template>
  <main class="stub">
    <p class="stub__label">Pendiente de construir</p>
    <h1>Eliminar duplicados de catálogo</h1>
  </main>
</template>

<style scoped>
.stub {
  max-width: 640px;
  margin: 0 auto;
  padding: var(--space-12) var(--space-4);
  text-align: center;
}

.stub__label {
  font-size: var(--text-xs);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--color-gray-500);
  margin: 0 0 var(--space-2);
}

.stub h1 {
  font-size: var(--text-xl);
  color: var(--color-navy-900);
  margin: 0;
}
</style>